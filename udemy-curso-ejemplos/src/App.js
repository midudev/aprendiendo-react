import React, { Component } from 'react'

/**
 * Este componente App está totalmente vacio, de forma
 * que os puede servir como base para utilizar cualquier otro componente
 * que queráis
 *
 * Por ejemplo, para probar el ejemplo de Renderizado Condicional
 * deberías importar el archivo de la siguiente forma:
 * import ConditionalRendering from './sections/conditional.js'
 *
 * Luego, en el render, deberías utilizar el componente así
 *   render () {
      return (
        <div className="App">
          <ConditionalRendering />
        </div>
      );
    }
 */

class App extends Component {
  render () {
    return (
      <div className="App">

      </div>
    );
  }
}

export default App;
